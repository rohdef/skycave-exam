/** 
 * Server side implementations of the domains central
 * roles, notably the Cave role (StandardServerCave) and
 * player (StandardServerCave). In addition the package contains
 * helper roles of architectural nature such as session caching
 * (PlayerSessionCache).
 * */
package cloud.cave.server;

