/**
 * Implementation of the domain for the client
 * side, in particular the Cave and Player interfaces.
 * <p/>
 * A classic Broker architecture is used with proxy
 * implementations of the major domain roles. However,
 * some simplifications are made as the domain is
 * rather simple for SkyCave.
 */
package cloud.cave.client;

