/**
 * Central roles for IPC = interprocess communication between the
 * client and server. The central two IPC abstracting roles are {@link ClientRequestHandler}
 * and {@link Invoker}.
 * The marshaling methods, marshaling to and from JSON are here as well, as are the IPC exception class.
 */
package cloud.cave.ipc;

